import 'package:flutter/material.dart';

/// Palette partagée par tout le Game Hub, pour que chaque jeu (y compris
/// les écrans internes comme les échecs) garde la même identité visuelle
/// que le hub : fond sombre + fond animé de particules + cartes "verre
/// dépoli".
const Color kBg = Color(0xFF050816);
const Color kSurface = Color(0xFF15152C);
const Color kAccent = Color(0xFF00DCFF);
const Color kMuted = Color(0xFF9AA6C7);
